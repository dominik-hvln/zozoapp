'use client';

import { OrderSummaryPageContent } from '@/app/(dashboard)/panel/zamowienie/[sessionId]/page';

export default function PublicOrderSummaryPage() {
  return <OrderSummaryPageContent isPublic />;
}
